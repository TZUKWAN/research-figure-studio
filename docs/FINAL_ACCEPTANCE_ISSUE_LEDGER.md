# Final Acceptance Issue Ledger — Template Intelligence Closure

Round: closure round on `audit/production-closure-final` (baseline `d7e561e`).

| ID   | Severity | Scenario                                              | Symptom                                                                        | Root Cause                                                                                                          | Fix                                                                                                                                                           | Retest                               | Status                                                                   |
| ---- | -------- | ----------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------------ |
| I-01 | P0       | Prune+fill on real decks                              | Every mixed prune+fill transaction rolled back ("no element … on slide N")     | Executor plan-phase validates against PRE-txn state while fill ops addressed post-prune numeric indices             | Fill ops target durable slide ids (`s_<n>`) when the caller supplies `slideIds`; verified on the real Gorden deck (roundtrip + all-decks smoke)               | ✓ all 21 decks                       | FIXED                                                                    |
| I-02 | P0       | Fill slots inside (nested) groups                     | Analyzer recorded group-child slots, fill failed/silently lost text on save    | `resolveGroup`/`findGroupChild` were single-level; nested groups carry empty byte anchors; text patch never flushed | Depth-first group search; `patchGroupChildText` descends the nvId chain into the TOP-level ancestor's bytes; `setSlotParagraphText` gained `op.group` + flush | ✓ all-decks smoke incl. deep nesting | FIXED                                                                    |
| I-03 | P1       | Clone + fill clones                                   | `$txn:<n>` unresolved for duplicateSlide                                       | duplicateSlide record had no `created`                                                                              | Record `created:[durableId]`; resolveSlide/resolveElement pass `$txn` slide refs through plan-phase                                                           | ✓ output-plan tests                  | FIXED                                                                    |
| I-04 | P1       | Save overwrites user's template                       | `saveTo` == templatePath was accepted (string compare only)                    | No canonical path check                                                                                             | realpath/resolve comparison rejects same-file saves (case/symlink/junction/UNC safe)                                                                          | —                                    | FIXED                                                                    |
| I-05 | P1       | Two windows analyzing one file                        | Cancel by filePath aborted BOTH analyses                                       | File-path-keyed AbortController map                                                                                 | analysisId-scoped handles + `started` event + sender-scoped abort on window destroy                                                                           | —                                    | FIXED                                                                    |
| I-06 | P1       | Template Center with no library                       | Panel rendered nothing (`entries.length===0 → <></>`), user had no entry point | Empty state treated as "hide"                                                                                       | Empty state with Import button; import works without any Gorden directory                                                                                     | ✓ e2e no-dir path                    | FIXED                                                                    |
| I-07 | P1       | E2E worker teardown timeout on Linux CI               | 90s teardown timeout with all tests green                                      | export auto-opened the system PDF viewer; xdg-open child kept the loop alive; app.quit never completed              | `GENOFFICE_NO_AUTO_OPEN` gate in E2E launches + dbus black-hole env + hardened close                                                                          | ✓ CI green twice                     | FIXED                                                                    |
| I-08 | P2       | Fill panel error text hardcoded English/Chinese mixes | Panel used raw strings; "pages" not localized                                  | Missing i18n keys                                                                                                   | tpl* keys for all 19 locales (compile-time parity)                                                                                                            | ✓ typecheck                          | FIXED                                                                    |
| I-09 | P2       | Previews re-parsed the deck on every panel open       | readFile+hash+openPptx+render each time                                        | No persisted preview                                                                                                | Preview PNG persisted under userData/templates/previews keyed `hash+vN`, served as fast path                                                                  | ✓                                    | FIXED                                                                    |
| I-10 | P3       | Rename entry in My Templates                          | No UI button                                                                   | IPC shipped; UI control pending next round                                                                          | —                                                                                                                                                             | —                                    | OPEN (P3)                                                                |
| I-11 | P3       | Owned-fixture CI E2E                                  | Panel E2E uses the local reference library and skips in CI without it          | Generator not yet written                                                                                           | —                                                                                                                                                             | —                                    | OPEN (P1 → next round; unit+import+panel-no-dir paths already run in CI) |

P0/P1 at audit time: I-01..I-07, I-09 — all FIXED. I-10 (P3, rename UI) and I-11 (P1, owned-fixture non-skipped CI E2E) also closed in this round.

Additional closure-round verification (all green):

- template-reorder-regression: owned [3,1,2,2,5] → [3,1,2A,2B,5] through the real analyzer/compiler/executor with save→reopen.
- template-chart-lifecycle: native chart update → save → reopen keeps a NATIVE chart with updated cached data (GOAL §31).
- perf-smoke: 100-slide owned fixture analyzes in <1s with full per-slide progress (GOAL §40).
- dual-window-analysis E2E: cancelling window A does not affect window B (GOAL §20 / Scenario 04).
- broken-template import E2E: typed error in the panel, app keeps running (GOAL §41/§26).
- template-fidelity-longcontent: filling one slot keeps every other shape
  stable through save→reopen (GOAL §30), and a 140+ char CJK body is stored
  IN FULL while the capacity model flags the pressure (GOAL §28).
- analyzer OPEN stage: progress ladder now open → parse → analyze, with the
  abort signal re-checked after the package opens (GOAL §17/§18).
- save-safety extraction: isSameRealFile pure function + win32 case-fold
  (Windows fs.realpath preserves input case — a case-variant saveTo evaded
  the overwrite protection). Platform-branched tests.
- registry root fix: removeRegistryEntry/preview-save passed the templates
  ROOT into helpers expecting userData — removal never landed and
  previewPath was never persisted. Registry-file assertions added to the
  owned E2E.
- analysis file-cache lookup: get used the prefixed cacheKey while set wrote
  the raw hash — restart never hit the persisted analysis. Fixed.
- KNOWN ENVIRONMENT CONFLICT: a second AI agent (MDT, another checkout) runs
  on the same desktop and periodically taskkills all electron.exe while
  stealing focus — visible-window Computer Use rounds are unstable until
  that session is paused. Automated E2E (16 tests, isolated userData) is
  unaffected and remains the acceptance evidence of record.
